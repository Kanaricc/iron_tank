#include <iostream>
#include <vector>
#include <fstream>
using namespace std;


vector<int> vec;
int main(){
    ios::sync_with_stdio(false);
    // ofstream fout("out.txt");
    // fout<<"???"<<endl;

    vector<int> vec;
    vec.resize(1e8);

    return 0;
}